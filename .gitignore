# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by `scripts/perf.mjs` — screenshots and a trace report, not source
perf-out

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
# Visual Studio's per-solution cache and file index.
.vs/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Runtime records written by the API, not source. The order book is trade, the
# analytics are a rolling window of counters — neither belongs in git.
src/data/orders.json
src/data/analytics.json
src/data/stock-intake.json
src/data/*.tmp
